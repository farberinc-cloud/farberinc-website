# Runtime data files (auto-created by api.php on the server)
leads.json
conversations.log
rate-limit.json
php-error.log
*.log
*.tmp
.DS_Store

# Real .env with secrets — copy .env.example to .env on the server (via SFTP)
.env
